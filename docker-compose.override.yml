services:
  web:
    build: .
    volumes:
      - ./pro_caida/scripts_docker:/code/scripts_docker
    environment:
      - ENVIRONMENT=development
  nginx:
    volumes:
      - ./nginx/dev/nginx.conf:/etc/nginx/conf.d/default.conf
      - static_data:/code/staticfiles
      - media_data:/code/media
  
  migrate_static:
    build: .
    command: sh /code/scripts_docker/dev/web_migrate.sh
    volumes:
      - ./pro_caida:/code
      - static_data:/code/staticfiles
      - media_data:/code/media
    environment:
      - MAKEMIGRATIONS=yes
      - MIGRATE=yes
      - STATIC=yes
      - ENVIRONMENT=development
    secrets:
      - SECRET_KEY
      - POSTGRES_DB
      - POSTGRES_USER
      - POSTGRES_PASSWORD
    networks:
      - backend